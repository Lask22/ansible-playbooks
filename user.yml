---
- name: Creating
  user:
    name: "{{ username }}"
    groups:
      - wheel
    state: present
  
- name: Add public key to authorized_keys
  authorized_key:
    user: "{{ username }}"
    state: present
    key: "{{ pubkey }}"
